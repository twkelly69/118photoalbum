# 118 Photo Album

靜態相簿頁面會展示 `photos/` 目錄內的所有圖片，並提供直接開啟與下載原圖的連結。

## 使用方式
1. 啟動開發伺服器或直接開啟 `index.html`。
   ```bash
   python -m http.server 8000
   ```
2. 在瀏覽器開啟 `http://localhost:8000` 觀看相簿。
3. 若要新增相片，將檔案放到 `photos/` 目錄並在 `index.html` 新增對應卡片即可。
